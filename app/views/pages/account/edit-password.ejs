<div id="edit-password" v-cloak>
  <div class="container">
    <h1>Cambiar Contraseña</h1>
    <hr/>
    <ajax-form action="updatePassword" :syncing.sync="syncing" :cloud-error.sync="cloudError" @submitted="submittedForm()" :handle-parsing="handleParsingForm">
      <div class="row">
        <div class="col-sm-6">
          <div class="form-group">
            <label for="current-password">Contraseña actual</label>
            <input class="form-control" id="current-password" name="current-password" type="password"  :class="[formErrors.currentPassword ? 'is-invalid' : '']" v-model.trim="formData.currentPassword" placeholder="••••••••">
            <div class="invalid-feedback" v-if="formErrors.currentPassword">Por favor ingrese una contraseña válida. La contraseña debe tener al menos una longitud de ocho (8) caracteres entre letras mayúsculas, letras minúscula y números.</div>
          </div>
        </div>
        <div class="col-sm-6">
          <div class="form-group">
            <label for="password">Nueva contraseña</label>
            <input class="form-control" id="password" name="password" type="password"  :class="[formErrors.password ? 'is-invalid' : '']" v-model.trim="formData.password" placeholder="••••••••">
            <div class="invalid-feedback" v-if="formErrors.password">Por favor ingrese una contraseña válida. La contraseña debe tener al menos una longitud de ocho (8) caracteres entre letras mayúsculas, letras minúscula y números.</div>
          </div>
        </div>
        <div class="col-sm-6">
          <div class="form-group">
            <label for="confirm-password">Confirmar contraseña</label>
            <input class="form-control" id="confirm-password" name="confirm-password" type="password"  :class="[formErrors.confirmPassword ? 'is-invalid' : '']" v-model.trim="formData.confirmPassword" placeholder="••••••••">
            <div class="invalid-feedback" v-if="formErrors.confirmPassword">Las contraseñas no coinciden.</div>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col-sm-6">
          <p class="text-danger" v-if="cloudError==='badCombo'">La contraseña actual es incorrecta, verifique que la contraseña ingresada corresponda a la contraseña actual de la cuenta.</p>
          <p class="text-danger" v-else-if="cloudError">Ha ocurrido un error inesperado mientras se ejecutaba la acción. <a href="/contact">Contacte con nosotros</a> si el error persiste.</p>
        </div>
        <div class="col-sm-6">
          <div class="form-group text-right">
            <a class="btn btn-outline-secondary" href="/account">Cancelar</a>
            <ajax-button type="submit" :syncing="syncing" class="btn btn-dark">Guardar cambios</ajax-button>
          </div>
        </div>
      </div>
    </ajax-form>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
